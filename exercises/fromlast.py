# --- Directions
# Given a linked list, return the element n spaces from the last node in the
# list.  Do not call the 'size' method of the linked list.  Assume that n will
# always be less than the length of the list.
# --- Examples
#    l = LinkedList()
#    l.insert_last('a')
#    l.insert_last('b')
#    l.insert_last('c')
#    l.insert_last('d')
#    from_last(l, 2).data # 'b'


def from_last(list, n):
    pass
