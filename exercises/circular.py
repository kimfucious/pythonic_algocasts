# --- Directions
# Given a linked list, return True if the list is circular, False if it is not.
# --- Examples
#   l = LinkedList()
#   a = Node('a')
#   b = Node('b')
#   c = Node('c')
#   l.head = a
#   a.next = b
#   b.next = c
#   c.next = b
#   circular(l) # True


def circular(l):
    pass
